# .github/workflows/ci.yml
name: Vite CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # Setup Node.js (adjust version as needed)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"

      # Install dependencies
      - name: Install dependencies
        run: npm ci

      # Run linter (if you use ESLint)
      - name: Run lint
        run: npm run lint --if-present

      # Run tests (if you use vitest or jest)
      - name: Run tests
        run: npm run test --if-present

      # Build project with Vite
      - name: Build project
        run: npm run build
